{{!< default}}
{{! The tag above means - insert everything in this file into the {body} of the default.hbs template }}
<div class="aviso" id="aviso">
        <div class="textos">
            
            <h2>Covid19Mexico</h2>    
            <p>
           
            Covid19mex.net es un periódico digital independiente que provee de manera gratuita 
            noticias sobre la pandemia provocada por el virus COVID-19. 
            Pero necesitamos tu ayuda, para mantenernos hablando con la verdad. 
            Considera donar, cualquier cantidad hace la diferencia.
            O subscribete y recibe los beneficios de la membresía.
            </p>

        </div>
        
        <div class="cerrar">
	 
               <span onclick="cerraranuncio('aviso')">{{> icons/close}}</span>
        </div>

        <div class="botones">            
            <button class="button button2">Donar</button>
            <button class="button button2">Subscribirse</button>     
        </div>
        
    </div>			

     
<div class="main-content-area">
 
 
    {{> homepage/featured-section}}
      
    <div class="container">
        <div class="row">
            <div class="col-lg-8">
                <div class="latest-post-section post-group">
                    <div id="contenido2">  
		    <div class="post-group-title">
                        <span>{{t "Ultimas Noticias"}}</span>
                    </div>
		    </div>
                    {{#foreach posts visibility="all"}}
                    {{#if @first}}
                    <div class="large-post clearfix">
                        {{#if feature_image}}
                        <a href="{{url}}">
                            <div class="featured-image-wrap">
                                <div class="featured-image" style="background-image:url({{feature_image}});"></div>
                            </div>
                        </a>
                        {{/if}}
                        <div class="content-wrap{{^if feature_image}} no-image{{/if}}">
                            <h2 class="title h4"><a href="{{url}}">{{title}}</a></h2>
                            <time class="date" datetime="{{date format="YYYY-MM-DD"}}" title="{{date format="DD MMMM, YYYY"}}">{{date format="MMMM DD, YYYY"}}</time>
                            <div class="excerpt">{{excerpt words="40"}}</div>
                        </div>
                    </div>
                    {{/if}}
                    {{/foreach}}
                    <div class="row">
                        {{#foreach posts visibility="all"}}
                        {{^if @first}}
                        <div class="col-md-6 small-post">
                            {{#if feature_image}}
                            <a href="{{url}}">
                                <div class="featured-image-wrap">
                                    <div class="featured-image" style="background-image:url({{feature_image}});"></div>
                                </div>
                            </a>
                            {{/if}}
                            <div class="content-wrap{{^if feature_image}} no-image{{/if}}">
                                <h2 class="title h5"><a href="{{url}}">{{title}}</a></h2>
                                <time class="date" datetime="{{date format="YYYY-MM-DD"}}" title="{{date format="DD MMMM, YYYY"}}">{{date format="MMMM DD, YYYY"}}</time>
                            </div>
                        </div>
                        {{/if}}
                        {{/foreach}}
                    </div>
                </div>
            </div>
            {{> homepage/tag-section-1}}
            {{> homepage/tag-section-2}}
            {{> homepage/tag-section-3}}
            {{> homepage/tag-section-6}}
            {{> homepage/tag-section-5}}
        </div>	    
	</div>
   

</div>
