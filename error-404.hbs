{{!< default}}
{{! The tag above means - insert everything in this file into the {body} of the default.hbs template }}
<div class="main-content-area">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="error-wrap text-center">
                    <h1 class="error-code">{{statusCode}}</h1>
                    <h2 class="error-message h2">{{message}}</h2>
                    <p class="message-manual">{{t "Desafortunadamente la página que estas buscando no puede ser encontrada."}}</p>
                    <a href="{{@site.url}}" class="btn btn-primary back-btn">{{t "Ir a la página principal"}} {{> icons/arrow-right}}</a>
                </div>
            </div>
        </div>
    </div>
</div>
